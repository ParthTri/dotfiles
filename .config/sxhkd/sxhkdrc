#!/usr/bin/env sh

## HYPER == SHIFT + CMD + ALT + OPTION

## open emacsclient
super + control e 
	emacsclient -c

## open firefox
super + control f 
	firefox

## Open discord
super + control d
	discord

## Rofi
super + d
	rofi -show drun

super + shift + d
	rofi -show run

## Powermenu
super + shift + q
	rofi -show power-menu -modi power-menu:~/.config/rofi/bin/rofi-power-menu

## Lock Screen
super + control + l
	~/.bin/lock.sh

## Resource Keyboard
super + control + r
  xmodmap ~/.Xmodmap 
